<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="gbpec.comida">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Registration_options" />
        <activity android:name=".RegistrationNGO" />
        <activity android:name=".RegistrationBusiness" />
        <activity
            android:name=".donor_module.Donor_NavigationMainActivity"
            android:label="@string/title_activity_donor__navigation_main"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>